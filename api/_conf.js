// Server side configartion file

var conf = {
    db: {
        host     : 'localhost',
        user     : 'root',
        password : '',
        database : 'ai-battle'
    }
};

module.exports = conf;